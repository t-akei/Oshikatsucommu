<div>
  <%= flash[:notice] %>
</div>
<%= render 'info', user: @user %>

<h4>Posts</h4>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Theme</th>
      <th>Text</th>
    </tr>
  </thead>
  <tbody>
    <% @posts.each do |post| %>
    <tr>
      <td><%= post.title %></td>
      <td><%= post.category %></td>
      <td><%= post.body %></td>
    </tr>
    <% end %>
  </tbody>
</table>