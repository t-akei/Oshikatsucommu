
<%= render 'info', user: @user %>

<h4>Posts</h4>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Genre</th>
      <th>Text</th>
    </tr>
  </thead>
  <tbody>
    <% @posts.each do |post| %>
    <tr>
      <td><%= post.title %></td>
      <td><%= post.genre_id %></td>
      <td><%= post.body %></td>
    </tr>
    <% end %>
  </tbody>
</table>