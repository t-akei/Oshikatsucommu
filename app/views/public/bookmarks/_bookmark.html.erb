<% if post.bookmarked_by?(current_user) %>
  <td><%= link_to '★', post_bookmarks_path(post), method: :delete %></td>
<% else %>
  <td><%= link_to '☆', post_bookmarks_path(post), method: :post %></td>
<% end %>