<div class="container mt-5">
  <h4 class="text-center"><%= @user.name %> さんのユーザーステータス変更</h4>
    <div class="row justify-content-center">
      <div class="col-md-6">
        <%= form_with model: @user, url: admin_user_path(@user), method: :patch do |f| %>
        <table class="table mt-3" rules="rows" border="3">
          <tbody>
            <tr>
              <th class="col-md-4" bgcolor="#DDDDDD">ユーザーID</th>
              <td class="col-md-2"></td>
              <td><%= @user.id %></td>
            </tr>
            <tr>
              <th class="col-md-4" bgcolor="#DDDDDD">ユーザー名</th>
              <td class="col-md-2"></td>
              <td><%= @user.name %></td>
            </tr>
            <tr>
              <th class="col-md-4" bgcolor="#DDDDDD">メールアドレス</th>
              <td class="col-md-2"></td>
              <td><%= @user.email %></td>
            </tr>
            <tr>
              <th class="col-md-4" bgcolor="#DDDDDD">ステータス</th>
              <td class="col-md-2"></td>
              <td>
                <%= f.radio_button :is_active, :true %>
                <%= f.label :is_active, "有効" %>
                <%= f.radio_button :is_active, :false %>
                <%= f.label :is_active, "無効" %><br>
                <%= f.submit "変更する" %>
              </td>
            </tr>
          </tbody>
        </table>
        <% end %>
      </div>
    </div>
</div>